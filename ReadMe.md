# Bunch of app dev archetypes

## spring-boot-camel

Spring Boot with camel and other usefule things.

```
mvn archetype:generate \
    -DarchetypeGroupId=com.itteddy.archetypes \
    -DarchetypeArtifactId=spring-boot-camel \
    -DarchetypeVersion=1.0.0
```